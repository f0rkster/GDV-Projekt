#pragma once

#include "yoshix_fix_function.h"

const float WIDTH = 12.0f;
const float HEIGHT = 9.0f;
const float LEFT_BORDER   = -(WIDTH / 2);
const float RIGHT_BORDER  =  (WIDTH / 2);
const float BOTTOM_BORDER = -(HEIGHT / 2);
const float TOP_BORDER    =  (HEIGHT / 2);